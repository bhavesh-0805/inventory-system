# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=inventory

# Frontend — set to your deployed backend URL for production builds
VITE_API_URL=http://localhost:8000
